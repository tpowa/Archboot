# Created by Tobias Powalowski <tpowa@archlinux.org>
# SPDX-License-Identifier: GPL-3.0-or-later

# SETUP archboot-cpio.sh parameters, other mkinitcpio parameters are not supported!

# Kernel is defined in this config file
_KERNEL="/boot/Image.gz"

_HOOKS=(common_modules block_modules init)

# COMPRESSION
# Use this to compress the initramfs image. By default, gzip compression
# is used. Use 'cat' to create an uncompressed image.
_COMPRESSION="zstd"
#_COMPRESSION="gzip"
#_COMPRESSION="bzip2"
#_COMPRESSION="lzma"
#_COMPRESSION="xz"
#_COMPRESSION="lzop"
#_COMPRESSION="lz4"

# COMPRESSION_OPTIONS
# Additional options for the compressor
_COMPRESSION_OPTIONS=(-19)
# vim: set ft=sh ts=4 sw=4 et:
