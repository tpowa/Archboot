# Created by Tobias Powalowski <tpowa@archlinux.org>

build ()
{
    MODULES=""
    BINARIES=""
    FILES=""
    SCRIPT="arch_ide_blacklist"
}

help ()
{
cat <<HELPEOF
  This hook will blacklist the pata ide subsystem, 
  if ide-legacy boot parameter is used.
HELPEOF
}
