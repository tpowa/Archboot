Showstoppers for next release:

New features:

Removed features:

Kernel changes:

hwdetect changes:

setup changes:

quickinst changes:

Planned Features:
- boot archboot iso via grub(2)'s loopback support
- Chroot installation
- Keymap changing during boot

Long term setup goals:
- Add automatic installation (perhaps not possible with archboot setup)
